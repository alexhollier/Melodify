import React from 'react';
import {ScrollView, Text} from 'react-native';

export default function Notation(){
    return(
        <ScrollView>
            <Text>
                Notation
            </Text>
        </ScrollView>
    );
}